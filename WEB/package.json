{
  "name": "lmarena-bridge-tg-backend",
  "version": "1.0.0",
  "description": "Backend for LMArena Bridge Telegram Mini App: verifies Telegram initData (holds BOT_TOKEN), issues sessions, and exposes CRUD for API keys / arena auth tokens (migrated from the FastAPI dashboard).",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  }
}

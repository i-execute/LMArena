// LMArena Bridge - Project Architecture Graph
// Generated by opencode

digraph LMArenaBridge {
    rankdir=TB;
    fontname="Arial";
    node [fontname="Arial", fontsize=10];
    edge [fontname="Arial", fontsize=8];
    
    // Title
    label="LMArena Bridge Architecture\nOpenAI-compatible API bridge to LM Arena";
    labelloc=t;
    fontsize=14;
    fontname="Bold";
    
    // Main Entry Point
    main [label="main.py\n(FastAPI App\n~4700 lines)", shape=box, style=filled, fillcolor="#4CAF50", fontcolor=white];
    
    // Core Modules
    subgraph cluster_core {
        label="Core Modules (src/)";
        style=dashed;
        color=blue;
        
        constants [label="constants.py\n(Hardcoded values\nURLs, selectors)", shape=box, style=filled, fillcolor="#E3F2FD"];
        config [label="config.py\n(Config I/O\nget_config, save_config)", shape=box, style=filled, fillcolor="#E3F2FD"];
        state [label="state.py\n(In-memory state\nchat sessions, tokens)", shape=box, style=filled, fillcolor="#E3F2FD"];
        auth [label="auth.py\n(Auth token management\nJWT, round-robin)", shape=box, style=filled, fillcolor="#E3F2FD"];
        recaptcha [label="recaptcha.py\n(reCAPTCHA v3/v2\nChrome/Camoufox)", shape=box, style=filled, fillcolor="#E3F2FD"];
        transport [label="transport.py\n(Transport layer\n4 fetch strategies)", shape=box, style=filled, fillcolor="#E3F2FD"];
        browser_utils [label="browser_utils.py\n(Browser utilities\nWindows/Playwright)", shape=box, style=filled, fillcolor="#E3F2FD"];
    }
    
    // Transport Strategies
    subgraph cluster_transport {
        label="Transport Strategies";
        style=dashed;
        color=orange;
        
        httpx [label="Direct httpx\n(Standard async HTTP)", shape=ellipse, style=filled, fillcolor="#FFF3E0"];
        chrome [label="Chrome fetch\n(Playwright browser\nfetch())", shape=ellipse, style=filled, fillcolor="#FFF3E0"];
        camoufox [label="Camoufox fetch\n(Firefox-based\nfallback)", shape=ellipse, style=filled, fillcolor="#FFF3E0"];
        userscript [label="Userscript proxy\n(Long-poll mechanism)", shape=ellipse, style=filled, fillcolor="#FFF3E0"];
    }
    
    // External Components
    subgraph cluster_external {
        label="External Components";
        style=dashed;
        color=red;
        
        lmarena [label="LM Arena\n(arena.ai)", shape=cylinder, style=filled, fillcolor="#FFEBEE"];
        cloudflare [label="Cloudflare\n(Protection)", shape=cylinder, style=filled, fillcolor="#FFEBEE"];
        recaptcha_ext [label="reCAPTCHA\n(Enterprise)", shape=cylinder, style=filled, fillcolor="#FFEBEE"];
        supabase [label="Supabase\n(Auth backend)", shape=cylinder, style=filled, fillcolor="#FFEBEE"];
    }
    
    // Frontend
    subgraph cluster_frontend {
        label="Frontend";
        style=dashed;
        color=purple;
        
        dashboard [label="Dashboard\n(HTML/Chart.js\nAdmin UI)", shape=box, style=filled, fillcolor="#F3E5F5"];
        web [label="WEB/\n(React/Node.js\nFrontend)", shape=box, style=filled, fillcolor="#F3E5F5"];
    }
    
    // BOT
    subgraph cluster_bot {
        label="BOT";
        style=dashed;
        color=green;
        
        bot [label="BOT/main.py\n(Telegram bot\nnotifications)", shape=box, style=filled, fillcolor="#E8F5E9"];
    }
    
    // DEPLOY
    subgraph cluster_deploy {
        label="DEPLOY";
        style=dashed;
        color=gray;
        
        deploy [label="DEPLOY/\n(Cloudflared\nDeployment)", shape=box, style=filled, fillcolor="#ECEFF1"];
    }
    
    // Connections
    main -> config [label="loads config"];
    main -> state [label="manages state"];
    main -> auth [label="uses auth"];
    main -> transport [label="selects transport"];
    main -> recaptcha [label="mints tokens"];
    main -> dashboard [label="serves"];
    
    config -> constants [label="reads"];
    auth -> recaptcha [label="refreshes"];
    auth -> supabase [label="refreshes via"];
    transport -> httpx [label="uses"];
    transport -> chrome [label="uses"];
    transport -> camoufox [label="uses"];
    transport -> userscript [label="uses"];
    
    httpx -> lmarena [label="requests"];
    chrome -> lmarena [label="requests"];
    chrome -> cloudflare [label="bypasses"];
    camoufox -> lmarena [label="requests"];
    camoufox -> cloudflare [label="bypasses"];
    userscript -> lmarena [label="requests"];
    
    chrome -> recaptcha_ext [label="solves"];
    camoufox -> recaptcha_ext [label="solves"];
    
    web -> main [label="API calls"];
    bot -> main [label="monitors"];
    deploy -> main [label="deploys"];
    
    // Legend
    subgraph cluster_legend {
        label="Legend";
        style=filled;
        fillcolor="#F5F5F5";
        
        key1 [label="Core Module", shape=box, style=filled, fillcolor="#E3F2FD"];
        key2 [label="Transport", shape=ellipse, style=filled, fillcolor="#FFF3E0"];
        key3 [label="External", shape=cylinder, style=filled, fillcolor="#FFEBEE"];
    }
}
